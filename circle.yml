machine:
  post:
    - pyenv global 2.7.9 3.4.2

dependencies:
    pre:
        - wget -O /home/ubuntu/bin/sixs http://rtwilson.com/downloads/sixs
        - chmod a+x /home/ubuntu/bin/sixs
        - pip install numpy
        - pip install nose

test:
    override:
        - tox
